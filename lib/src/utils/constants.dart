import '../../common.dart';

const kSearchFCN = 'search';
const kBorderRadius = 16.0;
const kPageSize = 20;

// Router constants
const kReturnTo = 'returnTo';
const appShellLabel = 'appShell';

// Shell navigation keys
final appShellNavigatorKey = GlobalKey<NavigatorState>(
  debugLabel: appShellLabel,
);

const kLogoPath = 'dashboard.png';
